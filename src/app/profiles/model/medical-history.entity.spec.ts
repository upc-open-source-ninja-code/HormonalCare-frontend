import { MedicalHistoryEntity } from './medical-history.entity';

describe('MedicalHistory', () => {
  it('should create an instance', () => {
    expect(new MedicalHistoryEntity()).toBeTruthy();
  });
});
