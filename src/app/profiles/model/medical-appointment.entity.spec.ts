import { MedicalAppointmentEntity } from './medical-appointment.entity';

describe('MedicalAppointment', () => {
  it('should create an instance', () => {
    expect(new MedicalAppointmentEntity()).toBeTruthy();
  });
});
